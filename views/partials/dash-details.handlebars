<section class="card">
  <a href="/post/{{id}}">
    <header>{{title}}</header>

    <div class="card-body">
      <p>{{content}}</p>
      <p>By: {{user.username}}</p>
      <p>Posted on: {{date_time}}</p>
    </div>
  </a>
  <button type="button" class="button" id='update' value={{id}}>Edit Post</button>
  <button type="button" class="button" id='delete' value={{id}}>Delete Post</button>
</section>