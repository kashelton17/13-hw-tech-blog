<section class="card">
  <a href="/post/{{id}}">
    <header>{{title}}</header>

    <div class="card-body">
      <p>{{content}}</p>
      <p>By: {{user.username}}</p>
      <p>Posted on: {{date_time}}</p>
    </div>
  </a>
</section>